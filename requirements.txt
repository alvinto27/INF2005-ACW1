# Runtime and test dependencies for the Flask application and masked-media protocol.
Flask==3.1.2
cryptography==50.0.1
av==15.1.0
numpy==2.5.3
# Pillow is used by tests and the demonstration notebook only; runtime image I/O uses PyAV.
pillow==12.3.0
Werkzeug==3.1.8
